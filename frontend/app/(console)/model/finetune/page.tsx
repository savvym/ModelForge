import { ConsolePage } from "@/components/console/console-page";

export default function ModelFinetunePage() {
  return (
    <ConsolePage
      pageKey="model-finetune"
      highlight="模型精调页建议下一步接入教程卡、任务表格和 Temporal 状态映射。"
    />
  );
}
